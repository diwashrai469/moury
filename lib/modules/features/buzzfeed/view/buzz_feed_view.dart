import 'package:flutter/material.dart';

class BuzzFeedView extends StatelessWidget {
  const BuzzFeedView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Buzz feed")),
    );
  }
}
